# API 2025 Project

This project is a Flask-based API that manages **specializations** and **course items**.  
It allows users to perform all CRUD (Create, Read, Update, Delete) operations using unique IDs.

## 📌 Features
- Create, read, update, and delete **specializations**
- Create, read, update, and delete **course items**
- Organized code using separate files (`app.py` and `db.py`)
- Uses UUIDs for unique identifiers
